name: Deploy site

on:
  push:
    branches:
      - master
      - main
    paths:
      - "assets/**"
      - "**.bib"
      - "**.html"
      - "**.js"
      - "**.liquid"
      - "**/*.md"
      - "**.yml"
      - "Gemfile"
      - "Gemfile.lock"
      - "!.github/workflows/axe.yml"
      - "!.github/workflows/broken-links.yml"
      - "!.github/workflows/deploy-docker-tag.yml"
      - "!.github/workflows/deploy-image.yml"
      - "!.github/workflows/docker-slim.yml"
      - "!.github/workflows/lighthouse-badger.yml"
      - "!.github/workflows/prettier.yml"
      - "!lighthouse_results/**"
      - "!CONTRIBUTING.md"
      - "!CUSTOMIZE.md"
      - "!FAQ.md"
      - "!INSTALL.md"
      - "!README.md"
  pull_request:
    branches:
      - master
      - main
    paths:
      - "assets/**"
      - "**.bib"
      - "**.html"
      - "**.js"
      - "**.liquid"
      - "**/*.md"
      - "**.yml"
      - "Gemfile"
      - "Gemfile.lock"
      - "!.github/workflows/axe.yml"
      - "!.github/workflows/broken-links.yml"
      - "!.github/workflows/deploy-docker-tag.yml"
      - "!.github/workflows/deploy-image.yml"
      - "!.github/workflows/docker-slim.yml"
      - "!.github/workflows/lighthouse-badger.yml"
      - "!.github/workflows/prettier.yml"
      - "!lighthouse_results/**"
      - "!CONTRIBUTING.md"
      - "!CUSTOMIZE.md"
      - "!FAQ.md"
      - "!INSTALL.md"
      - "!README.md"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    # available images: https://github.com/actions/runner-images#available-images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Check image changes
        id: image-changes
        run: |
          IMAGEMAGICK_BUILD=true
          if git rev-parse --verify HEAD^1 >/dev/null 2>&1; then
          if git diff --quiet HEAD^1 -- assets/img; then
            IMAGEMAGICK_BUILD=false
          fi
          fi
          echo "enable_imagemagick=$IMAGEMAGICK_BUILD" >> "$GITHUB_OUTPUT"
      - name: Check purgecss necessity
        id: purgecss-needed
        run: |
          PURGECSS=true
          if git rev-parse --verify HEAD^1 >/dev/null 2>&1; then
            CHANGED_FILES=$(git diff --name-only HEAD^1 HEAD)
            if ! echo "$CHANGED_FILES" | grep -Eq "\.css$|\.scss$|^_sass/|^_layouts/|^_includes/|^assets/css/"; then
              PURGECSS=false
            fi
          fi
          echo "run_purgecss=$PURGECSS" >> "$GITHUB_OUTPUT"
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2.2"
          bundler-cache: true
      - name: Update _config.yml ⚙️
        uses: fjogeleit/yaml-update-action@main
        with:
          commitChange: false
          valueFile: "_config.yml"
          propertyPath: "giscus.repo"
          value: ${{ github.repository }}
      - name: Install and Build 🔧
        run: |
          export JEKYLL_ENV=production
          if [[ "${{ steps.image-changes.outputs.enable_imagemagick }}" == "true" ]]; then
            sudo apt-get update
            sudo apt-get install -y --no-install-recommends imagemagick
            bundle exec jekyll build
          else
            bundle exec jekyll build --config _config.yml,_config.no-imagemagick.yml
          fi
      - name: Purge unused CSS 🧹
        if: steps.purgecss-needed.outputs.run_purgecss == 'true'
        run: |
          npm install -g purgecss
          purgecss -c purgecss.config.js
      - name: Deploy 🚀
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: _site
